Types of e-commerce models

1. Business to Consumer(B2C)
2. Business to Business (B2B)
3. Consumer to Consumer (C2C)
4. Consumer to Business (C2B)


actions from admin part
- add product
- delete product
- update product
- view transactions
- soft delete

actions from users part
- add product to cart 
- view cart items
- update product (increase, decrease)
- delete item in a cart
- empty cart
- register

for both admin and user 
- login
- logout
- view all products
- view single products

data model 
- products 
    - id
    - name
    - price
    - image
    - description
    - quantity

- users 
    - id
    - username
    - fullname
    - password
    - isAdmin: false

- transactions
    - id
    - username
    - items [
        {
            productId: quantity,
            productId: quantity
        }
    ]

    - total
    - dataPurchased
    - payment
    - status = 'pending', 'cancelled', 'completed'

    Delete Product Button
    <a href="/controllers/process_delete_product.php?=<?php echo $product['id']; ?>" class="btn btn-danger">Delete</a>

    inside delete controller

    <?php
    $json_file = file_get_content('../data/products.json');
    $products = json_decode($json_file, true);
    
    $products = array_filter($products, function($product) {
        return $product['id'] != $_GET['id'];
    });

    file_put_contents('../data/products.json', json_encode($products, pretty print));
    header('Location: /');
    